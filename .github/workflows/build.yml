name: mi primer workflow

on:
  push:
    branches: [main]
jobs:
  checkout:
    runs-on: ubuntu-latest
  steps:
    - name: checkout
      uses: actions/checkout@v4
    - name: pailita
      run: echo "hola paila!"