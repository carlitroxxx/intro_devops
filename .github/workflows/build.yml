name: mi primer workflow

on:
  push:
    branches: [main]
  jobs:
    checkout:
      runs-on: ubuntu-latest
    steps:
      - name: Checkout nazi
        uses: actions/checkout@v4
      - name: pailita
        run: echo "hola paila!"